export enum SettingTypes {
    "select",
    "colour",
    "toggle"
}

export enum SettingNames {
    "difficulty" = "Difficulty",
    "duration" = "Duration",
    "size" = "Sizes",
    "cursor" = "Cursor",
    "colour" = "TargetColour",
    "sound" = "Sound"
}